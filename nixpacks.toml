[start]
cmd = "mkdir -p backend/public && cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"

[build]
cmd = "cd frontend && npm install && npm run build && mkdir -p ../backend/public && cp -r dist/* ../backend/public/"