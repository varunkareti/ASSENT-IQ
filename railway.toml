[build]
builder = "NIXPACKS"

[deploy]
startCommand = "mkdir -p backend/public && cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"

[env]
PYTHON_VERSION = "3.11.0"